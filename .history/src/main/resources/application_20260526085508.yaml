spring:
  application:
    name: SuperMarket


  datasource:
    url: jdbc:mariadb://localhost:3306/supermarket
    username: root
    password: guayaba12
    driver-class-name: org.mariadb.jdbc.Driver  

  jpa:
    hibernate:
      ddl-auto: update
